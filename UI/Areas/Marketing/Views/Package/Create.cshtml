@using Falcon.App.UI.HtmlHelpers
@model Falcon.App.Core.Marketing.ViewModels.PackageEditModel
@{
    ViewBag.Title = "Create";
}
<h1>
    Create Package</h1>
<div class="option-links">
    <a href="/Marketing/Package/">Manage Packages </a>
</div>
@if (Model != null)
{
    @Html.Partial("Message", new ViewDataDictionary(Model.FeedbackMessage))
}
@{Html.BeginForm("Create", "Package", FormMethod.Post, new { enctype = "multipart/form-data" });} 
@Html.EditorForModel()
<div class="submit-button-row">
    @Html.ActionLink("Close", "Index", "Package") &nbsp;&nbsp; @Html.SubmitButton("Create Package")
</div>
@{Html.EndForm();}
