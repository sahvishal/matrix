@using Falcon.App.UI.HtmlHelpers
@using Falcon.App.Core.Users.ViewModels

@model MedicalVendorEditModel
@{
    ViewBag.Title = "Create Medical Vendor";
}

<h2>Create Medical Vendor</h2>

@if (Model != null)
{
    @Html.Partial("Message", new ViewDataDictionary(Model.FeedbackMessage))
}

@{Html.BeginForm("Create", "MedicalVendor", FormMethod.Post, new { enctype = "multipart/form-data" });}    
    
    @Html.EditorForModel()

    <div class="submit-button-row">
        @Html.ActionLink("Close", "Index", "MedicalVendor")&nbsp;&nbsp; @Html.SubmitButton("Create Medical Vendor")
    </div>

@{Html.EndForm();}