@using Falcon.App.UI.HtmlHelpers

@model Falcon.App.Core.Operations.ViewModels.VanEditModel
@{
    ViewBag.Title = "Create New Vehicle";
}

<h1>Add New Vehicle</h1>
<div class="option-links">
    <a href="/App/Common/VanDetails.aspx"> Manage Existing Vehicles </a>
</div>

@if (Model != null)
{
    @Html.Partial("Message", new ViewDataDictionary(Model.FeedbackMessage))
}


<fieldset>
    <legend>Vehicle Details</legend>
     @{Html.BeginForm();}

        @Html.EditorForModel() 
        
        
        @Html.ActionLink("Close","Index","Van") &nbsp;&nbsp; @Html.SubmitButton("Create Vehicle")
        
     @{Html.EndForm();}
 </fieldset>